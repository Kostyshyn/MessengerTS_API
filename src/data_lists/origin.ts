// If you need to sort by field, you need to add it to the list

export const origin = [
  'name',
  'origin_url',
  'api_key',
  'isDefault',
  'updatedAt',
  'createdAt'
];

export const origins = [
  'name',
  'origin_url',
  'api_key',
  'isDefault',
  'updatedAt',
  'createdAt'
];

export default {
  origin,
  origins
}
