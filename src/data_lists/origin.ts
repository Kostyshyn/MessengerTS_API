// If you need to sort by field, you need to add it to the list

export const origin = [
  'name',
  'origin',
  'api_key'
];

export const origins = [
  'name',
  'origin',
  'api_key',
  'createdAt'
];

export default {
  origin,
  origins
}
